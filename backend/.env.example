### Copy this file to `.env` and fill values before running the backend.
# Do NOT commit your real .env. This example is safe to commit.

MONGODB_URI=mongodb+srv://<user>:<password>@cluster0.example.mongodb.net/chat-app?retryWrites=true&w=majority
JWT_KEY=replace_with_a_secure_random_value
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# Development notes:
# On Windows PowerShell you can generate a secure random JWT key without openssl.
# Example (PowerShell):
#   [Convert]::ToBase64String((New-Object Security.Cryptography.RNGCryptoServiceProvider).GetBytes(32))
# Or using Node.js (if installed):
#   node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
